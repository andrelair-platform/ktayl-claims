<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.3.5</version>
    <relativePath/>
  </parent>

  <groupId>com.andrelair.ktayl</groupId>
  <artifactId>claims-acl</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>ktayl-claims-acl</name>
  <description>Modern Claims capability built AS the ACL / strangler over the GlobalCore legacy (ADR-001/006)</description>

  <properties>
    <java.version>21</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- Stack per ADR-006 — wired in as the slice is built:
         · spring-ws / JAX-WS  → SOAP client to GlobalCore (S003/S004)
         · spring-boot-starter-data-jpa + ojdbc / Oracle read-critical checks (S004)
         · spring-boot-starter-data-jpa + PostgreSQL → CQRS read-model (S006)
         · NATS client → CDC event consumer (S005/S006)
         Kept out of v0 so the app boots with no external datastore. -->

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <!-- Allow Mockito/Byte Buddy to run on a JDK newer than it officially supports
               (local dev may be on JDK 24+; CI pins Java 21 where this is a no-op). -->
          <argLine>-Dnet.bytebuddy.experimental=true</argLine>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
